import org.junit.jupiter.api.Test;

import java.util.HashMap;

import static org.junit.jupiter.api.Assertions.*;

class TodoListSaveListTest {
    @Test
    void test_saveTodoList() {
        //Test that contents of standard items array have been copied into map, so they can be recalled later
        //Use assertEquals to test contents
    }
}